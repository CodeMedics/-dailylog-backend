debug: false
management.endpoints.web.exposure.include: "*"

logging:
  level:
    org.springframework.web.servlet: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

server:
  port: 8080

spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: c546ff9f2eee86018135486e22c019a9
            client-secret: nQ2sWRCUnsi9dbv17e4xxFI73Lpy33eu
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: POST
            client-name: Kakao
            scope:
              - profile_nickname
              - profile_image
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/harulog
    username: harulog
    password: harulog
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  thymeleaf:
    cache: false # Set to true for production; false for development (disabled caching for faster development)
    mode: HTML5
    encoding: UTF-8


  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 100
    show-sql: true

  sql.init.mode: always

  data.rest:
    base-path: /api
    detection-strategy: annotated

  thymeleaf3.decoupled-logic: true

jwt:
  secret: 13128391823091czxcz123kj12h3kjh12kj451kj2h3kj1h251kj23kj123kj