<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ohgiraffers.dailylogbackend.feed.query.infra.mapper.GetFeedMapper">
    <select id="getFeedList" resultType="List">
        SELECT *
        FROM FEED F
        RIGHT 0UTER JOIN DIARY D
        ON F.DIARY_ID = D.DIARY_ID
    </select>
</mapper>